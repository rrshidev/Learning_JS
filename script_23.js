'use strict'
//function teme

/*function checkAge(age) {
  return (age > 18) ? true : prompt('А родители разрешили?',);    
}*/

/*function checkAge(age) {
  return (age > 18) || confirm('Mom is  ready?');
  }
}*/

/*let a = prompt('Введите число А:');
let b = prompt('Введите число B:');


function min(a, b) {
  if (a < b) {
    return a;
  } 
  return b;
}

alert(min(a, b));
*/

let x = prompt('Введи число:');
let n = prompjat('Введи степень:');

function pow(x, n) {
  return (x**n);
}

alert(pow(x, n));