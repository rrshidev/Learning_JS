'use strict'
let lang_name = prompt('How is official language name of JS ', '');

if (lang_name == 'ECMAScript') {
    alert('F8ck yeah!')
}
else if (lang_name != 'ECMAScript') {
    alert("Don't know? It's name is ECMAScript!")
}
